general:
  build_dir: TribesOfLagopus/app/
  
dependencies:
  override:
    - chmod +x gradlew
    - ./gradlew dependencies
    
deployment:
  production:
    branch: master
    commands:
      - ./deploy_master.sh
